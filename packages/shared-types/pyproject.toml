[project]
name = "shared-types"
version = "0.1.0"
description = "Shared types for proposal-diagram-copilot"
requires-python = ">=3.11"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/shared_types"]
